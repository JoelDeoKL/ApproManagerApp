{% extends 'template.html.twig' %}

{% block title %}ApproManager - Liste Inspecteur{% endblock %}

{% block body %}

  <div class="main-panel">
    <div class="content-wrapper">
      <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
          <div class="card">
            <div class="card-body">
              <h4 class="card-title">Liste Approvisionneur</h4>
              <div class="table-responsive">
                <table class="table table-striped">
                  <thead>
                  <tr>
                    <th>Nom</th>
                    <th>Postnom</th>
                    <th>Prenom</th>
                    <th>Telephone</th>
                    <th>Actions</th>
                  </tr>
                  </thead>
                  <tbody>
                    {% for approvisionneur in approvisionneurs %}
                      <tr>
                        <td>{{ approvisionneur.nom }}</td>
                        <td>{{ approvisionneur.postnom }}</td>
                        <td>{{ approvisionneur.prenom }}</td>
                        <td>{{ approvisionneur.telephone }}</td>
                        <td>
                          <a href="" class="btn btn-primary">Voir</a>
                          <a href="{{ path("app_register", {id: approvisionneur.id}) }}" class="btn btn-primary">Modifier</a>
                          <a href="" class="btn btn-primary">Supprimer</a>
                        </td>
                      </tr>
                    {% endfor %}
                  </tbody>
                </table>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
{% endblock %}